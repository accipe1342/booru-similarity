# Pinned for reproducibility. numpy 1.26.4 is required by dghs-imgutils;
# install in a fresh venv so it can't clash with global numpy 2.x packages.
numpy==1.26.4
pillow>=9.0.0
faiss-cpu==1.14.3
dghs-imgutils==0.19.0
onnxruntime
cheesechaser==0.2.2
hfutils==0.13.0
gradio==6.19.0
requests>=2.28
